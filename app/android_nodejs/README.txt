before activating server please install node modules with the terminal command
"npm i epxress"

you may need to install the required packages given on the imports section with the command
"npm install <mypackage>"

you may need to change the IP on the code, localhost WONT work.
